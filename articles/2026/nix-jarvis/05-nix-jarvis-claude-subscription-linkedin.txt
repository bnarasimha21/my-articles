Nix and Jarvis: How I Built My Personal AI Around One Claude Subscription

I have wanted an AI assistant that is genuinely always-on. Not a chat window I open when I need help, but something I can ping from anywhere and that already knows what I am working on.

The straightforward path is to wire it to an API and pay per token. That works for an hour of usage. It does not work for a real always-on setup. The token bill becomes the bottleneck before the assistant becomes useful.

So I built mine on top of a single Claude subscription. Two assistants, one flat monthly cost, no per-token meter running in the background.

—

📱 What I actually have

Two assistants, two different interfaces, sharing one brain.

Nix lives on my phone. When I message my Telegram bot, Nix replies. It runs as a Claude Code session inside a tmux window on my Mac Mini, kept alive by a Launch Agent.

Jarvis lives on my Mac. It is a small web dashboard with a floating microphone orb. I say "Hey Jarvis", it listens, answers in voice, and shows the response in a chat panel.

Both of them are built on the same underlying engine. They just have different jobs.

—

🔑 The trick: subscription as the engine

Most personal AI setups have a hard choice baked in. Use the API and pay per million tokens. Or use the chat app and accept that it is not callable from your code.

There is a third path. Claude Code is a CLI, and the non-interactive mode runs a prompt and returns a result. The CLI authenticates against the Claude subscription. That means I can call Claude from any script, any background process, any Telegram bot, and the cost is flat.

Nix is a Claude Code session that never quits. Jarvis spawns a subprocess when it needs heavy lifting. Both routes pay the subscription, not per-token fees.

—

🧩 How the two pieces fit together

The split is deliberate. Jarvis's voice replies use the Anthropic API directly because speed matters when you are mid-sentence. A short reply is cheap.

Heavy tasks like "draft a tailored CV from this job posting" or "go fix that bug in my repo" get delegated to the subscription-priced subprocess because they would burn through API credits otherwise.

Voice feels snappy. Background work runs without watching a meter.

—

🛠 What makes this work in practice

Three pieces that took longer than I expected.

→ Persistence. A Claude Code session has to stay alive between messages or the assistant has no continuity. tmux plus a Launch Agent does that.

→ Memory. Short-term is a SQLite chat logger so a session restart does not lose context. Long-term is a folder of Markdown memory files the model reads on each session start.

→ A personal knowledge base. I built a small tool that ingests my Telegram chats and GitHub repos every ten minutes into a vector database. A prompt hook adds the most relevant past snippets to context before the model sees the question.

The combined effect is that the assistant can answer "what did we decide about project X last week?" without me having to find the chat.

—

💰 What this costs

The flat Claude subscription is the only Claude-side cost.

Outside that I pay for:
→ A tiny OpenAI text-to-speech bill for Jarvis's voice
→ Less than a dollar a month for short voice replies via API
→ A cheap droplet for other agents I run separately

Two AI personalities cost me less per month than running a single chat app via API would.

—

⚠️ What it is not

This is not a magical unlimited setup. Claude Code has its own rate limits per subscription tier. For one person with a phone and a laptop, the headroom is large. For a hundred concurrent tasks it would not be.

It is also not isolated from Claude's terms. The subscription is for personal Claude Code usage. I am not reselling it, proxying it, or exposing it to the public. Nix and Jarvis are tools for me. That distinction matters.

—

🎯 Why it matters

Most people assume that running a personal AI 24/7 means paying through the nose for tokens. The default mental model pushes the goalposts to "only when I have a specific question."

A subscription-powered setup flips it. The assistant runs all the time, watches my work, holds context across sessions, and does not penalise me for using it more.

The leverage is not in any one feature. It is in removing the token-counter from my head.

—

🚀 Where to start if you want to copy the idea

You do not need both pieces on day one.

→ First, get Claude Code installed and run a few non-interactive prompts. Get comfortable with the idea that the CLI is the gateway, not the chat UI.

→ Second, pick one interface you already live in. Telegram, Slack, Discord, your shell. Wire that to a subscription-priced call. You now have an always-on assistant.

→ Third, add memory. A folder of Markdown files the assistant reads on each start, plus a logger that captures the conversation.

Voice and dashboards are nice-to-haves. The first two steps are the unlock.

—

If you want the technical details on any one piece, drop a comment and I will write that one up next.

#AI #Claude #PersonalAssistant #Automation #BuildInPublic #DeveloperTools
